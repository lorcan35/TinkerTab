---
title: Skill SDK
sidebar_label: Skill SDK
---

# Skill SDK

:::info Coming soon
Write a skill: declare a `Tool` subclass with name, description, args schema, capability tags. Implement `execute(args, ctx) -> result`. Optionally emit widgets via `ctx.surface.emit(...)`. Register in `dragon_voice/tools/__init__.py`.

This page is a stub. Real content lands in PR 2 (user-facing) or PR 3 (developer-facing) per the docs-site plan.
:::
