---
title: Module map
sidebar_label: Module map
---

# Module map

:::info Coming soon
`dragon_voice/` is the entry point. Submodules: `api/` (REST), `llm/` (backend abstraction), `tools/` (skills), `surfaces/` (widget emitters), `messages.py` (store), `conversation.py` (engine), `server.py` (WS dispatcher), `scheduler/`, `media/`.

This page is a stub. Real content lands in PR 2 (user-facing) or PR 3 (developer-facing) per the docs-site plan.
:::
