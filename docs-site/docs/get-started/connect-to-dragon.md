---
title: Connect to Dragon
sidebar_label: Connect to Dragon
---

# Connect to Dragon

:::info Coming soon
By default Tab5 finds Dragon over mDNS (`tinkerclaw.local`). Manual host:port can be set in Settings → Network. Falls back to ngrok tunnel if the LAN host is unreachable.

This page is a stub. Real content lands in PR 2 (user-facing) or PR 3 (developer-facing) per the docs-site plan.
:::
