---
title: Video calling stack
sidebar_label: Video calling stack
---

# Video calling stack

:::info Coming soon
Both directions framed `"VID0" + len + JPEG`. Audio framed `"AUD0" + len + PCM`. Dragon is a dumb broadcast relay (zero codec work). Tab5 atomic helpers `voice_video_start_call()` / `voice_video_end_call()`.

This page is a stub. Real content lands in PR 2 (user-facing) or PR 3 (developer-facing) per the docs-site plan.
:::
