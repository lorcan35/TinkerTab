---
title: LLM backends
sidebar_label: LLM backends
---

# LLM backends

:::info Coming soon
Five backends — `ollama` (local), `openrouter` (cloud), `lmstudio` (LAN), `npu_genie` (Q6A NPU), `tinkerclaw` (gateway). Capability declarations published as a fleet for per-modality routing.

This page is a stub. Real content lands in PR 2 (user-facing) or PR 3 (developer-facing) per the docs-site plan.
:::
