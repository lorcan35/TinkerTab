---
title: Voice-mode config
sidebar_label: Voice-mode config
---

# Voice-mode config

:::info Coming soon
Each voice mode picks a (STT, LLM, TTS) tuple. Defaults are sane; override via `config.yaml` → `voice_modes:` block. Tab5's `vmode` NVS key picks the active tier on the device.

This page is a stub. Real content lands in PR 2 (user-facing) or PR 3 (developer-facing) per the docs-site plan.
:::
