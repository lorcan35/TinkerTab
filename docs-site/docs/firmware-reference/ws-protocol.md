---
title: WebSocket protocol
sidebar_label: WebSocket protocol
---

# WebSocket protocol

:::info Coming soon
Tab5 ↔ Dragon JSON over a single WS. Tab5 sends register / start / stop / cancel / text / config_update / clear / ping. Dragon sends session_start / stt / llm / tool_call / tool_result / tts_start / media / config_update / error / pong. Binary frames use VID0/AUD0 magic prefixes.

This page is a stub. Real content lands in PR 2 (user-facing) or PR 3 (developer-facing) per the docs-site plan.
:::
