[pytest]
addopts = -v --strict-markers --tb=short
markers =
    baseline: sanity tests against current firmware (always should pass)
    ui: UI tests that depend on overhaul features (home, keyboard, etc.)
    slow: tests that take >10s
    hardware: requires physical device
testpaths = .
python_files = test_*.py
